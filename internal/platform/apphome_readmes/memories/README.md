# Memories

This folder stores custom memory files that BlazeAI may load into prompts when relevant.

- Each loadable memory should be a Markdown file with `[DESCRIPTION]` and `[DETAILS]` sections.
- Keep descriptions short and details focused on durable facts, not transient task steps.
- `README.md` documents the folder only and must not be treated as a loadable memory.
