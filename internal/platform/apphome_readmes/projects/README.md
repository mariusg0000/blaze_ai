# Projects

This folder stores project-scoped BlazeAI runtime data grouped by working folder.

- Each project gets its own subfolder derived from the working directory path.
- Session history for that project lives inside its `sessions/` subtree.
- The top-level `README.md` explains the structure; individual project folders are runtime data, not hand-maintained docs.
