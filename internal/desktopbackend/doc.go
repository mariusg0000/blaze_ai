// doc.go — Electron desktop backend package.
// Hosts the active desktop transport state, transcript, local commands, and
// stdio protocol server without any WebView or native window dependencies.
// Layer: desktop backend. Dependencies: runtime, session, config, platform.
package desktopbackend
