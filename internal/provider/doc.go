// provider/doc.go — OpenAI-compatible provider client and model role management.
// Handles HTTP communication with OpenAI-compatible endpoints, streaming responses (SSE),
// tool-call parsing, and token usage reporting for compaction triggers.
// Layer: external API client. Dependencies: internal/config, internal/session.
package provider
