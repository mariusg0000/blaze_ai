Archived Go WebView desktop transport reference.

This directory keeps the removed Linux WebView desktop implementation intact so the
current Electron migration can reuse its HTML, CSS, JavaScript, state model, and
transport behavior as reference.

It is intentionally excluded from the main module build. Do not import it from the
active application.
